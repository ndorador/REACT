import { BsCart } from "react-icons/bs"; 

const CartWidget = () => {
  return (
    <div className="CartWidget">
      <BsCart size={22} color="white" /> 
    
    </div>
  );
};

export default CartWidget;